name: ref 

# Allow repository modyfication;
permissions:
  contents: write
  pull-requests: write
  packages: write

on:
  # Trigger automaticly;
  push:
    branches:
      - '*' 
  # Trigger manually;
  workflow_dispatch:

jobs:
  tagging:
    uses: utilizable/github-actions/.github/workflows/tagging_semver.yml@develop
    secrets:
      token: ${{ secrets.TOKEN }}
